[package]
name = "lamportian-dramatis"
version = "0.3.0"
compiler = "0.14.0"
entrypoint = "src/lib.typ"
authors = ["Manuel Vázquez Acosta <https://github.com/mvaled>"]
license = "MIT"
description = "Draw Lamport diagrams of replicas exchanging messages (with all the drama it entails)."
homepage = "https://lamportian-dramatis.github.io/"
repository = "https://github.com/lamportian-dramatis/package"
categories = ["visualization"]
disciplines = ["computer-science"]
keywords = [
  "lamport",
  "lamport diagram",
  "lamport clocks",
  "happens-before",
  "distributed systems",
  "event sourcing",
  "causality",
  "concurrency",
]
exclude = ["/gallery/*"]
